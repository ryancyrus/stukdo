

<%= form_for(@task, :html => {class: 'ui form'}) do |f| %>
  <% if @task.errors.any? %>
    <div class="ui message">
      <div class="header"><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</div>

      <ul class="list">
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="field">
    <%= f.label :duedate %><br>
    <%= f.date_select :duedate %>
  </div>
  
    <%= f.submit :value => 'Create Task', :class => 'ui primary button' %>
  
<% end %>

